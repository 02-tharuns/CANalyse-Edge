[project]
name = "canalyse-edge"
version = "0.1.0"
description = "CAN-to-predictive-diagnostics gateway with an SOVD-inspired API"
requires-python = ">=3.12,<3.13"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q -p no:cacheprovider --basetemp=.test-tmp-local"

